/**
 * Created by alexcatalisan on 03.11.2017.
 */

/* Based on defined routes, build routing manager */
export function buildRoutingManager(routes) {
  return {};
}

/* Based on component configuration create the component reducer */
export function buildComponentReducer(component) {
  return {};
}

/* After creating the component reducer, attach it to store to have it globally */
export function attachReducerToStore(reducer) {
  return {};
}

/* Wholw dependecy tree */
export function setComponentsDependencies(dependecyTree) {
  return {};
}

/* Set component dependency based on defined dependencies in dependecyTree */
export function setComponentDependency(component, dependency) {
  return {};
}
